apiVersion: v1
kind: Service
metadata:
  name: {{ include "gotifyme.fullname" . }}
  labels:
    {{- include "gotifyme.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "gotifyme.selectorLabels" . | nindent 4 }}
